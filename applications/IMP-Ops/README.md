# IMP-Ops

**Status in `v2026.08-dq`:** indexed, not bundled as a release asset.

IMP-Ops is treated as a ReIG-adjacent AI operation model. It organizes AI operation into layered roles and an Independent Mobile Personal function, but it is not part of the ReIG-DQ core release package.
