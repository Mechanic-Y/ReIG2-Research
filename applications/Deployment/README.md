# Deployment dynamics

**Status in `v2026.08-dq`:** indexed, not bundled as a release asset.

The deployment dynamics work models AI field deployment as a two-layer Kuramoto-type system. It is relevant to ReIG-style operational theory, but its current version is better treated as a separate experimental / model-focused paper line.
